import React, { useState, useEffect } from "react";

const StatisticsCard = () => {
/*   const [statistics, setStatistics] = useState({
    total_users: 0,
    total_cars: 0,
    rented_cars: 0,
    unrented_cars: 0,
  });

  useEffect(() => {
    const fetchStatistics = async () => {
      try {
        const response = await fetch('http://127.0.0.1:5000/api/statistics');
        const data = await response.json();
        setStatistics(data);
      } catch (error) {
        console.error('Error fetching statistics:', error);
      }
    };

    fetchStatistics();
  }, []);

  return (
    <div className="p-6">
      <h2 className="text-2xl font-bold mb-6">Statistics</h2>
      <table className="min-w-full table-auto border-collapse">
        <thead>
          <tr className="bg-gray-100">
            <th className="px-4 py-2 text-left">Statistic</th>
            <th className="px-4 py-2 text-left">Value</th>
          </tr>
        </thead>
        <tbody>
          <tr className="border-b">
            <td className="px-4 py-2 font-semibold">Total Users</td>
            <td className="px-4 py-2">{statistics.total_users}</td>
          </tr>
          <tr className="border-b">
            <td className="px-4 py-2 font-semibold">Total Cars</td>
            <td className="px-4 py-2">{statistics.total_cars}</td>
          </tr>
          <tr className="border-b">
            <td className="px-4 py-2 font-semibold">Rented Cars</td>
            <td className="px-4 py-2">{statistics.rented_cars}</td>
          </tr>
          <tr className="border-b">
            <td className="px-4 py-2 font-semibold">Unrented Cars</td>
            <td className="px-4 py-2">{statistics.unrented_cars}</td>
          </tr>
        </tbody>
      </table>
    </div>
  ); */
}
export default StatisticsCard;
